[project]
name = "zarr-datafusion-search-root"
version = "0.1.0"
description = "Zarr datafusion search"
readme = "README.md"
requires-python = ">=3.11"

[dependency-groups]
dev = ["ipykernel>=6.30.1"]
docs = [
  "mkdocs-material>=9.5",
  "mkdocstrings[python]>=0.29",
]

[tool.uv.workspace]
members = [
    "python",
]
