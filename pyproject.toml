[project]
name = "zarr-datafusion-internal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.2",
    "shapely>=2.1.1",
    "zarr>=3.1.2",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
]
